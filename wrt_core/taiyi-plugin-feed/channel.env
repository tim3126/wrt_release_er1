# Set both values and add the matching public-key.pem in this directory to
# enable the signed Taiyi add-on feed in a reviewed firmware build. These are
# intentionally blank until a stable HTTPS host and dedicated public key exist.
TAIYI_PLUGIN_FEED_INDEX_URL=
TAIYI_PLUGIN_FEED_PUBLIC_KEY_SHA256=
